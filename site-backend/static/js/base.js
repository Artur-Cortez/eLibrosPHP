document.addEventListener('DOMContentLoaded', function() {

    inputs = document.querySelectorAll('.adicionar_x');

    inputs.forEach(function(input) {
        input.addEventListener('click', function() {
            
        });
    }
});